#angular-google-books

This library is intended to wrap the Google books api(https://developers.google.com/books/) as an angular module.

The build process requires npm, grunt, and bower. 

##Testing
`grunt test`